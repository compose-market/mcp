# MCP Container Registry

Automated containerization of ~1,710 MCP servers from the official registry.

## Packages

All containers are published to `ghcr.io/compose-market/mcp/{server-name}` as **public packages**.

## Building

The containerization runs via GitHub Actions. Go to the [Actions tab](https://github.com/compose-market/mcp/actions) and run the workflow.
